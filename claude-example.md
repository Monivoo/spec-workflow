# CLAUDE.md 示例：基于规格文档的 Claude Code 开发约束

下面内容可复制到目标项目根目录的 `CLAUDE.md` 中，用于约束 Claude Code CLI 在开发前读取并遵循 `spec-workflow` 生成的规格文档。

```md
# CLAUDE.md

本项目采用规格文档驱动开发。Claude Code 在修改代码、生成代码、重构、补测试或调整配置前，必须先读取并遵循对应版本的规格文档。

## 开发前必须确认版本

如果用户没有明确说明本次开发基于哪个版本，必须先询问：

> 本次开发基于哪个规格版本？例如 `docs/prd/v1.2.0/`。

不得在版本不明确时直接实现功能。

## 开发前必须阅读的文档

在实现任何功能前，必须先阅读对应版本目录下的文档：

- `docs/prd/<version>/prd.md`
- `docs/prd/<version>/design.md`
- `docs/prd/<version>/dev.md`
- `docs/prd/<version>/plan.md`
- `docs/prd/<version>/test.md`

如果项目包含运维或发布变更，还必须阅读：

- `docs/prd/<version>/ops.md`
- `docs/prd/CHANGELOG.md`
- `docs/prd/README.md`

## 规格文档优先级

当用户请求、代码现状和规格文档存在差异时，按以下顺序处理：

1. 用户本轮明确指令
2. 对应版本的规格文档
3. 根目录 `CLAUDE.md`
4. 当前代码实现

如果不同文档之间存在冲突，必须先向用户说明冲突点并等待确认，不得自行选择一种方案继续实现。

## 实现边界

- 不得实现 `prd.md` 未定义的功能。
- UI、页面结构、交互流程和组件行为必须以 `design.md` 为准。
- 架构、数据模型、接口、数据库变更和文件影响必须以 `dev.md` 为准。
- 任务拆解、里程碑和风险处理必须参考 `plan.md`。
- 测试范围、验收标准和回归检查必须以 `test.md` 为准。
- 部署、环境变量、监控、回滚和故障排查必须以 `ops.md` 为准。

## 修改代码后的同步要求

完成实现后，必须检查是否需要同步更新文档：

- 需求范围变化：更新 `prd.md`
- 页面或交互变化：更新 `design.md`
- 接口、数据结构、架构或数据库变化：更新 `dev.md`
- 任务状态、风险或排期变化：更新 `plan.md`
- 测试策略、测试用例或验收标准变化：更新 `test.md`
- 部署、配置、监控或回滚变化：更新 `ops.md`
- 用户可见变更、迁移说明或破坏性变更：更新 `CHANGELOG.md`
- 版本状态变化：更新 `docs/prd/README.md`

## 开发流程

每次开发任务按以下流程执行：

1. 确认规格版本。
2. 阅读该版本的规格文档。
3. 总结本次任务的实现边界和验收标准。
4. 如规格不完整或冲突，先询问用户。
5. 实现代码。
6. 补充或更新测试。
7. 运行必要验证。
8. 检查并同步更新相关文档。
9. 汇报改动内容、验证结果和未完成事项。

## 推荐任务提示词

用户可以使用以下提示触发规格驱动开发：

```text
请基于 docs/prd/v1.2.0/ 下的规格文档实现用户登录功能。
实现前先阅读 prd.md、design.md、dev.md、plan.md、test.md。
不要实现规格外功能。
如果规格文档之间存在冲突，先询问我。
完成后同步更新测试和相关文档。
```

## 强制校验建议

`CLAUDE.md` 是项目级协作规则，但不是技术强制。对于必须执行的团队规则，建议额外使用：

- Claude Code hooks
- Git hooks
- CI 检查
- PR checklist

例如：

- 修改 API 文件但未更新 `dev.md` 时阻止提交。
- 修改页面组件但未更新 `design.md` 或 `test.md` 时给出警告。
- PR 必须填写对应规格版本和验收结果。
```
