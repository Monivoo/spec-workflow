# spec-workflow 介绍 PPT 细化文档

## PPT 定位

- **目标受众**：公司内正在使用或准备使用 AI Coding 的产品、研发、测试、运维、技术管理者。
- **核心目标**：让团队理解 AI Coding 的主要瓶颈不是“模型不会写代码”，而是“上下文不稳定、需求不清晰、验证不可追踪”；推广使用 `spec-workflow` 建立文档驱动的 AI 协作开发流程。
- **建议时长**：20–30 分钟。
- **建议页数**：15 页。
- **主线叙事**：AI Coding 痛点 → 如何用规格化流程解决 → spec-workflow 是什么 → 它如何支撑团队协作 → 功能能力介绍 → 落地推广。

---

## 第 1 页：封面

- **标题**：用 spec-workflow 让 AI Coding 从“写代码”走向“可控交付”
- **副标题**：面向团队协作的文档驱动 AI 开发工作流
- **页面内容**：
  - spec-workflow v3.0.0
  - 关键词：结构化需求 / 版本化文档 / AI 稳定上下文 / 可追踪交付
- **设计建议**：采用品牌感较强的封面风格，标题大字居中或偏左，副标题作为次级说明，关键词做成底部标签或横向信息条。
- **布局建议**：上半区放主标题和副标题；中下区放版本号；底部横向排列 4 个关键词标签。背景可使用淡色网格、流程线条或抽象技术图形，但不能干扰标题识别。
- **讲解重点**：这不是单纯介绍一个 Skill，而是推广一套让 AI Coding 在团队里可复用、可审计、可落地的工作方式。

## 第 2 页：当下 AI Coding 的核心痛点

- **标题**：AI Coding 的问题，往往不在“写代码”本身
- **页面内容**：
  - **需求层**：
    1. **需求描述零散**：一句话需求、口头讨论、聊天记录混在一起，AI 很容易只理解局部信息。
    2. **边界容易被脑补**：没有明确“不做什么”“优先级是什么”“验收标准是什么”时，AI 会自行补全业务规则。
    3. **交付结果不可验证**：看起来代码完成了，但缺少从需求到验收、测试、上线检查的闭环。
  - **协作层**：
    4. **上下文不稳定**：同一个项目在不同会话、不同成员、不同阶段反复解释背景，AI 对项目规则缺少长期记忆。
    5. **跨角色理解不一致**：产品关注目标，研发关注实现，测试关注验证，运维关注上线，但缺少统一文档承接。
    6. **代码、文档、测试不同步**：代码改了，但 PRD、技术方案、测试用例、运维说明没有同步更新。
  - **工程层**：
    7. **技术方案脱离真实项目**：AI 可能按通用模板生成方案，没有先理解当前仓库结构、依赖版本、代码组织方式。
    8. **版本不可追溯**：迭代后难以回答“这个版本为什么做、做了什么、改动影响哪里、如何回滚”。
- **设计建议**：使用“三层问题卡片”结构，不要把 8 条痛点平铺成一长列；通过分组降低阅读压力，并突出“问题不是写代码，而是上下文与协作机制”。
- **布局建议**：一页三栏或上中下三块，分别对应“需求层 / 协作层 / 工程层”；每块放组标题和 2–3 条短句，配小图标辅助识别。标题下可加一条强调语：`问题不在生成代码，而在生成可交付结果。`
- **讲解重点**：AI Coding 的核心挑战不是单次 prompt 技巧，而是团队是否有稳定、明确、可追踪的上下文和交付协议。

## 第 3 页：针对这些痛点，我们应该怎么解决

- **标题**：用规格化流程约束 AI，而不是让 AI 自由发挥
- **页面内容**：
  - **需求先结构化**：先澄清目标用户、核心场景、成功标准、优先级和不做范围，再进入实现。
  - **文档先确认**：先确认 `prd.md`，再进入设计和技术方案，避免需求没定就写代码。
  - **方案基于仓库**：技术方案必须读取真实项目结构、配置文件、核心代码和现有模块后再生成。
  - **验证提前定义**：在规格阶段同步定义测试策略、验收标准、部署前检查，而不是开发结束后补测试。
  - **版本持续沉淀**：每次新增、修复、重构都进入版本化目录，并同步 `CHANGELOG.md` 和版本索引。
  - **AI 上下文长期化**：通过 `CLAUDE.md` 沉淀项目背景、技术栈、架构决策、开发规范和 AI 协作规则。
- **设计建议**：这一页要呈现“解决路径”，适合流程化表达，不建议继续使用纯项目符号列表。
- **布局建议**：采用 6 步横向流程图或 2 行 3 列卡片布局；每个点保留标题和一句解释。可在页底加一句总结：`把临时对话变成可持续协作流程。`
- **讲解重点**：解决 AI Coding 痛点的关键，是把“隐性沟通”变成“显性规格”，把“临时对话”变成“可持续协作流程”。

## 第 4 页：spec-workflow 是什么

- **标题**：spec-workflow：面向 AI 协作开发的文档驱动工作流 Skill
- **页面内容**：
  - **一句话定义**：`spec-workflow` 是一个面向 AI 协作开发的文档驱动工作流 Skill。
  - **它做什么**：通过结构化、版本化的项目文档，把需求、设计、实现、测试、运维和迭代过程串联起来。
  - **它解决什么问题**：为 AI Coding 提供稳定上下文，提升开发的准确性、可控性与可追溯性。
  - **它关注什么**：不是“模型会不会写代码”，而是需求是否清晰、边界是否明确、技术方案是否可执行、验证标准是否完整。
  - **它带来的结果**：把模糊想法沉淀为可确认、可追踪、可迭代的开发上下文，让 AI 沿着明确规格参与完整开发流程。
- **工作流总览**：
  - 阶段 0：需求澄清与方案拷问。
  - 阶段 1：生成 `prd.md`，等待用户确认。
  - 阶段 2：生成 `design.md` / `dev.md`，等待技术方案确认。
  - 阶段 3：生成 `plan.md` + `test.md` + `ops.md`。
  - 阶段 4：更新 `CHANGELOG.md`、`CLAUDE.md` 和版本索引。
- **设计建议**：这一页是概念定义页，需要同时回答“它是什么”和“它怎么运作”。
- **布局建议**：左侧放 5 条定义信息，右侧放 5 阶段工作流；或采用上下分区布局：上半区定义，下半区流程。避免整页全是正文段落。
- **讲解重点**：spec-workflow 不是普通模板库，而是一套带确认门禁、文档边界、版本治理和验证闭环的 AI 工作流。

## 第 5 页：spec-workflow 如何解决这些痛点

- **标题**：从痛点到机制：spec-workflow 的解决路径
- **页面内容**：
  | AI Coding 痛点 | spec-workflow 的机制 | 结果 |
  |---|---|---|
  | 需求零散 | 需求澄清 + `prd.md` | 把想法变成可确认需求 |
  | 上下文不稳定 | 版本化文档 + `CLAUDE.md` | AI 和团队共享同一套上下文 |
  | AI 自行脑补 | 优先级、验收标准、不做范围 | 减少偏航和过度实现 |
  | 技术方案脱离项目 | 生成 `dev.md` 前审查仓库 | 方案贴合真实代码结构 |
  | 测试滞后 | `test.md` + PRD ↔ Code 双向验证 | 从需求到测试形成闭环 |
  | 上线风险高 | `ops.md` + 回滚 + 监控告警 | 上线、运维、排障有依据 |
  | 版本难追溯 | 语义化版本 + `CHANGELOG.md` + 索引 | 版本变化可审计、可回溯 |
- **设计建议**：这一页最适合表格，不建议改成长段落；重点是让观众一眼看到“问题—机制—结果”的映射关系。
- **布局建议**：居中放三列表格，占页面主体宽度；表头高亮，左列可加浅色标签区分。页底可加一句结论：`spec-workflow 的价值在于把痛点转化为机制。`
- **讲解重点**：它不是简单“生成几份文档”，而是用文档把需求、代码、测试、上线、迭代这些环节连接起来。

## 第 6 页：这个 Skill 在团队协作中的价值

- **标题**：任何角色拉下仓库，都能基于文档完成自己的工作
- **页面内容**：
  - **产品经理**：通过 `prd.md` 看到版本目标、用户需求、功能范围、优先级、验收标准。
  - **研发团队**：
    - 前端工程师通过 `design.md` 看到页面结构、交互流程、组件拆分、界面约束。
    - 后端工程师通过 `dev.md` 看到架构方案、数据模型、接口影响清单、文件变更范围、数据库脚本。
  - **测试工程师**：通过 `test.md` 看到测试策略、测试用例、集成场景、静态扫描和部署前检查。
  - **运维 / DevOps**：通过 `ops.md` 看到环境变量、部署步骤、监控告警、故障排查和回滚方案。
  - **技术负责人 / 管理者**：通过 `CHANGELOG.md` 和版本索引看到每个版本的目标、变更、风险和状态。
  - **AI 助手**：通过 `CLAUDE.md` 获得项目上下文、技术栈、规范、架构决策和协作规则。
- **补充说明**：同一套版本化文档，既服务人，也服务 AI；减少口头同步、重复解释和跨角色信息丢失。
- **设计建议**：适合做“角色 → 文档价值”的映射页，重点体现协作覆盖面。
- **布局建议**：使用 6 个角色卡片或 2 列卡片墙布局；每张卡片包含角色名、对应文档、收获内容。可将“AI 助手”卡片用强调色突出，体现主题差异化。
- **讲解重点**：spec-workflow 让仓库不仅有代码，还有完成工作所需的上下文；新成员或 AI 拉取代码后，不需要从零问背景，可以直接根据对应文档进入角色任务。

## 第 7 页：需求澄清能力

- **标题**：先把问题问清楚，再开始生成文档
- **页面内容**：
  - 当需求不完整、目标不清晰、边界模糊时，不直接生成文档。
  - 采用一问一答方式澄清关键决策。
  - 每个问题提供推荐答案，减少用户决策成本。
  - 优先澄清目标用户、核心场景、成功标准、P0/P1/P2、明确不做范围、风险约束。
- **设计建议**：这一页适合做“提问式交互”视觉，让观众感知到 AI 不是直接输出，而是先做结构化澄清。
- **布局建议**：左侧放 4 条能力点，右侧放一个简化对话框示意或问答气泡区。也可用“问题 → 推荐答案”两栏形式表现交互感。
- **讲解重点**：这能避免 AI 在需求上游就走偏，也能帮助提出需求的人把想法结构化。

## 第 8 页：技术方案必须基于真实项目

- **标题**：避免 AI 基于模板假设生成脱离项目的方案
- **页面内容**：
  - 生成 `dev.md` 前必须审查项目：
    - 目录结构
    - 配置文件与依赖版本
    - 核心入口文件
    - 相关模块现有实现
    - 命名规范和组织模式
  - 后端项目必须包含接口影响清单。
  - 数据库变更必须生成独立 SQL 文件，如 `sql/DDL.sql`、`sql/DML_init.sql`。
- **设计建议**：这一页要传达“先读仓库，再出方案”的工程约束感。
- **布局建议**：左侧放“审查项目”检查清单，右侧放“输出结果”两块：接口影响清单、SQL 文件。可加入文件树或代码片段风格的弱背景元素。
- **讲解重点**：AI 不是凭空设计系统，而是基于现有代码和约束做可执行方案。

## 第 9 页：测试驱动闭环

- **标题**：不是写完代码再补测试，而是规格阶段就定义验证方式
- **页面内容**：
  - `test.md` 包含测试策略、单元测试清单、集成测试场景、静态扫描规则、部署前检查。
  - 任何代码变更必须同步更新测试。
  - 支持 PRD ↔ Code 双向验证：
    - PRD → 代码：需求是否都有实现。
    - 代码 → PRD：代码是否有需求依据。
- **设计建议**：这一页核心是“闭环”，视觉上要能看出验证链路。
- **布局建议**：采用圆环图、双向箭头图或 3 段流程：`PRD → Code → Test → 验收`。正文放在图形旁边，不要只用文字堆砌。PRD ↔ Code 可单独做双向箭头强调。
- **讲解重点**：这能减少“AI 写了很多代码，但没人知道是否满足需求”的情况。

## 第 10 页：版本治理与迭代管理

- **标题**：让每次 AI 参与的变更都可追溯
- **页面内容**：
  - **支持语义化版本**：
    - **Major**：重大版本，包含破坏性变更、架构调整、核心流程重构或不兼容升级。
    - **Minor**：次要版本，包含兼容性新增功能、能力扩展、流程增强或模块级优化。
    - **Patch**：修订版本，包含 Bug 修复、文案调整、小范围体验优化或非破坏性补丁。
  - **支持的版本动作**：新增版本、扩展已有版本、查看版本状态、版本对比、跨版本搜索、归档旧版本。
  - **自动维护**：根据变更类型建议版本号，并同步维护 `CHANGELOG.md` 和 `docs/prd/README.md` 版本索引。
- **补充说明**：版本管理记录的不只是代码变化，更是业务范围、优先级、风险和交付状态的变化。
- **设计建议**：这一页重点是“版本规则 + 版本动作”，适合分层展示，避免把所有信息塞进一个列表。
- **布局建议**：上半区做 Major / Minor / Patch 三张对比卡片，下半区放“支持的版本动作”和“自动维护”两块说明。可以加入版本号递增示意，如 `v1.0.0 → v1.1.0 → v1.1.1`。
- **讲解重点**：团队不只需要生成当前代码，还需要知道版本之间业务上增加了什么、删除了什么、优先级如何变化。

## 第 11 页：运维与事故沉淀

- **标题**：把上线后的风险也纳入 AI 协作上下文
- **页面内容**：
  - `ops.md`：环境变量、部署步骤、监控告警、故障排查、回滚方案。
  - `incident.md`：事故概要、时间线、影响评估、根因分析、5 Why、预防措施。
  - 生产环境项目上线前必须补充运维和测试文档。
- **设计建议**：这一页要体现“上线前”和“出问题后”两个阶段，适合用双区块对照。
- **布局建议**：左侧放 `ops.md`，右侧放 `incident.md`，分别配部署和告警/事故图标；底部单独放一条强调语：`生产环境项目必须补齐运维与测试文档。`
- **讲解重点**：AI Coding 不能只关注“功能能跑”，还要关注“能上线、能监控、能回滚、能复盘”。

## 第 12 页：文档职责边界与完整生命周期

- **标题**：每份文档只回答一个问题，合起来覆盖完整交付链路
- **页面内容**：
  | 文档 | 回答的问题 | 所属阶段 |
  |---|---|---|
  | `prd.md` | 做什么、为什么 | 需求阶段 |
  | `design.md` | 长什么样 | 设计阶段 |
  | `dev.md` | 如何实现 | 开发阶段 |
  | `plan.md` | 何时做、谁来做 | 计划阶段 |
  | `test.md` | 如何验证 | 测试阶段 |
  | `ops.md` | 如何部署和运维 | 运维阶段 |
  | `CHANGELOG.md` | 版本间变更 | 迭代阶段 |
  | `CLAUDE.md` | AI 可读项目总纲 | 协作阶段 |
- **页面补充**：
  - 不把技术方案写进 `prd.md`；不把用户故事写进 `dev.md`。
  - 不把数据模型和接口定义写进 `plan.md`；不把部署流程写进 `dev.md`。
  - 不把测试用例写进 `prd.md`。
- **补充说明**：文档边界越清晰，AI 越不容易把需求、设计、实现、测试和运维内容混写在一起。
- **设计建议**：这页最适合表格或泳道式结构，重点是让观众快速理解“文档职责边界”。
- **布局建议**：上半区放 8 行职责表格，下半区放 3 条“不要混写”的规则提示。表格必须占主体视觉位置，规则提示可做成浅色说明框。
- **讲解重点**：文档边界清晰能减少重复、冲突和上下文污染；完整生命周期让 AI 和团队从规划到运维都有依据。

## 第 13 页：使用方式与操作示例

- **标题**：怎么用 spec-workflow：从一句需求到完整文档链路
- **页面内容**：
  1. **安装 Skill**：将 `spec-workflow` 放入 Claude Skills 目录，例如 `~/.claude/skills/spec-workflow` 或 `%USERPROFILE%\.claude\skills\spec-workflow`。
  2. **在目标项目中启动 Claude Code**：进入需要生成文档的项目仓库，让 AI 能读取真实目录结构和代码上下文。
  3. **输入需求或指令**：用 `/spec-workflow` 描述版本目标、业务场景、用户角色、功能范围、优先级和约束。
  4. **先确认 PRD**：AI 先澄清需求并生成 `prd.md`，用户确认后再进入设计和技术方案。
  5. **继续生成完整链路**：根据项目类型生成 `design.md`、`dev.md`、`plan.md`、`test.md`、`ops.md`、`CHANGELOG.md`、`CLAUDE.md`。
  6. **纳入版本管理**：将生成的版本目录、变更日志和项目上下文随代码一起评审和维护。
- **示例 Prompt**：
  - `/spec-workflow 我想做用户认证版本，包含账号密码登录、短信验证码登录、注册、找回密码；先完成登录注册闭环，再补安全设置和异常登录提醒，请先帮我澄清需求并生成 PRD。`
- **补充说明**：这页既展示“怎么触发”，也展示“生成后如何进入确认—扩展—维护”的完整链路。
- **设计建议**：这一页最适合做“操作步骤 + 示例输入”结构，便于观众理解实际使用方式。
- **布局建议**：左侧放 6 步流程，右侧放示例 Prompt 代码框；Prompt 区建议用深浅对比底色，做成终端或输入框样式。
- **讲解重点**：使用方式不是“让 AI 直接写代码”，而是先让 AI 把需求、方案、验证、运维和版本记录沉淀下来，再基于确认后的规格进入实现。

## 第 14 页：让 AI 开发从“参考文档”变成“遵循规范”

- **标题**：让后来接手的人和 AI，都先读规格再动代码
- **页面内容**：
  1. **先确认版本**：如果用户没有说明基于哪个版本开发，Claude Code 必须先询问，例如 `docs/prd/v1.2.0/`。
  2. **先读规格文档**：实现前必须阅读对应版本的 `prd.md`、`design.md`、`dev.md`、`plan.md`、`test.md`，涉及发布时还要读 `ops.md`、`CHANGELOG.md` 和版本索引。
  3. **明确优先级**：用户本轮明确指令优先，其次是对应版本规格文档，再是根目录 `CLAUDE.md`，最后才是当前代码实现。
  4. **限制实现边界**：不得实现 `prd.md` 未定义的功能；UI 以 `design.md` 为准；接口、数据结构、数据库和文件影响以 `dev.md` 为准；验收以 `test.md` 为准。
  5. **改完同步文档**：代码改动后检查是否需要同步更新 PRD、设计、技术方案、测试、运维、变更日志和版本索引。
- **示例片段**：
  ```md
  本项目采用规格文档驱动开发。Claude Code 在修改代码、生成代码、重构、补测试或调整配置前，必须先读取并遵循对应版本的规格文档。
  
  如果用户没有明确说明本次开发基于哪个版本，必须先询问：
  > 本次开发基于哪个规格版本？例如 docs/prd/v1.2.0/。
  
  ## 开发前必须阅读的文档
  
  在实现任何功能前，必须先阅读对应版本目录下的文档：
  
  - `docs/prd/<version>/prd.md`
  - `docs/prd/<version>/design.md`
  - `docs/prd/<version>/dev.md`
  - `docs/prd/<version>/plan.md`
  - `docs/prd/<version>/test.md`
  
  不得在版本不明确时直接实现功能。
  ```
- **推荐任务提示词**：
  - `请基于 docs/prd/v1.2.0/ 下的规格文档实现用户登录功能。实现前先阅读 prd.md、design.md、dev.md、plan.md、test.md。不要实现规格外功能。如果规格文档之间存在冲突，先询问我。完成后同步更新测试和相关文档。`
- **设计建议**：这一页信息较多，必须分层展示，不能把正文、示例片段和 Prompt 全部堆成一列。
- **布局建议**：上半区放 5 条规则，采用编号卡片或规则列表；下半区左右分栏，左侧放 `CLAUDE.md` 示例片段代码框，右侧放推荐任务提示词。可在标题下增加小字强调：`把协作规则写进仓库。`
- **讲解重点**：`CLAUDE.md` 是把团队协作规则写进仓库的方式。Claude Code 不会天然自动严格遵循所有规格文档，但可以通过根目录 `CLAUDE.md`、固定 Prompt / Slash Command、hooks / CI，把“按规格开发”变成可复用、可检查的团队流程。

## 第 15 页：推荐落地方式与行动建议

- **标题**：从一个版本开始，把 spec-workflow 跑起来
- **页面内容**：
  1. **试点阶段**：选择一个新功能或小版本，用 spec-workflow 生成完整文档链路。
  2. **协作阶段**：产品确认 PRD，研发确认 dev.md，测试补充 test.md，运维确认 ops.md。
  3. **固化阶段**：把 `docs/prd/`、`CHANGELOG.md`、`CLAUDE.md` 纳入 PR 检查范围。
  4. **推广阶段**：沉淀优秀版本样例，形成团队内部标准。
  5. **持续阶段**：每次增量需求、Bug 修复、重构、上线变更都同步更新对应文档。
- **补充说明**：建议从一个真实版本试点，不追求一开始覆盖所有团队，而是先跑通一个可复用样板。
- **设计建议**：收尾页要有行动感，不适合再做复杂表格，重点是“下一步怎么做”。
- **布局建议**：使用 5 步路线图或时间轴，从“试点”到“持续”逐步推进；底部可加一句行动号召，例如：`从下一个明确版本开始试点。`
- **讲解重点**：推广目标不是增加流程负担，而是减少沟通成本、返工成本和上线风险；建议从下一个明确版本开始试点。

---

## 附录：可用于 PPT 的一句话卖点

- spec-workflow 是一套让 AI Coding 从“临时对话”变成“工程化协作”的文档驱动工作流。
- 它通过 PRD、设计、技术、计划、测试、运维、变更日志和 AI 项目记忆体，建立稳定上下文。
- 它用确认门禁和版本治理降低 AI 脑补、需求偏航、测试缺失和交付不可追溯的风险。
- 它适合新项目启动、新版本规划、增量迭代、测试补齐、运维上线和事故复盘等场景。

## 附录：建议演示 Demo

- **Demo 主题**：用 spec-workflow 为一个“用户认证 v0.3.0”版本生成文档链路。
- **演示步骤**：
  1. 输入一段相对零散的需求。
  2. 展示 AI 先进行需求澄清，而不是直接生成代码。
  3. 生成 `prd.md` 并等待确认。
  4. 确认后生成 `design.md` / `dev.md`。
  5. 再生成 `plan.md`、`test.md`、`ops.md`、`CHANGELOG.md`、`CLAUDE.md`。
  6. 展示版本索引如何记录该版本。
- **演示重点**：让观众看到“AI 不只是写代码，而是在被工作流约束下参与完整交付”。
